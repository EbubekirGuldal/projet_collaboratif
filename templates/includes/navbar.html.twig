<nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top">
	<div class="container">

		<a class="navbar-brand fw-semibold d-flex align-items-center gap-2" href="{{ path('app_home') }}">
			<i class="fa-solid fa-hand-holding-heart text-primary"></i>
			<span>Ressources relationnelles</span>
		</a>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="mainNavbar">

			<form class="nav-search d-flex flex-grow-1 mx-lg-4 my-3 my-lg-0" role="search" method="get" action="{{ path('app_home') }}">
				<div class="input-group nav-search__group w-100">
					<span class="input-group-text nav-search__icon">
						<i class="fa-solid fa-magnifying-glass"></i>
					</span>

					<input
					class="form-control nav-search__input" type="search" name="q" value="{{ app.request.query.get('q') }}" placeholder="Rechercher une ressource (ex: santé, aide, démarches...)" aria-label="Search">

					<button class="btn btn-primary nav-search__btn d-none d-lg-inline-flex" type="submit">
						Rechercher
					</button>
				</div>
			</form>

			<div class="d-flex align-items-center gap-2 ms-lg-2">
				{% if app.user %}
					<a class="btn btn-outline-primary" href="{{ path('app_profile_saved') }}">
						<i class="fa-regular fa-bookmark me-2"></i>
						Enregistrés
					</a>

					<a class="btn btn-primary" href="{{ path('app_profile') }}">
						<i class="fa-solid fa-user me-2"></i>
						Profil
					</a>

					<a class="btn btn-outline-primary" href="{{ logout_path() }}">
						<i class="fa-solid fa-right-from-bracket me-2"></i>
						Déconnexion
					</a>
				{% else %}
					<a class="btn btn-outline-primary" href="{{ path('app_login') }}">
						<i class="fa-solid fa-right-to-bracket me-2"></i>
						Connexion
					</a>

					<a class="btn btn-primary" href="{{ path('app_register') }}">
						<i class="fa-solid fa-user-plus me-2"></i>
						Inscription
					</a>
				{% endif %}

				{% if is_granted('ROLE_ADMIN') %}
					<a href="{{ path('admin') }}" class="btn btn-outline-primary btn-sm d-flex align-items-center gap-2 px-3 rounded-pill shadow-sm">
						<i class="fa-solid fa-shield-halved"></i>
						<span>Admin</span>
					</a>
				{% endif %}
			</div>

		</div>
	</div>
</nav>